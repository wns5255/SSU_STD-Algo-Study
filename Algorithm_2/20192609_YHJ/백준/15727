#include <iostream>
using namespace std;

int main(void)
{
	int distance;

	cin >> distance;

	if (distance >= 1 && distance <= 1000000)
	{
		if((distance % 5 )==0)
			cout << distance / 5;
		
		if ((distance % 5) != 0)
			cout << distance / 5 + 1;
	}
		
	return 0;
}
