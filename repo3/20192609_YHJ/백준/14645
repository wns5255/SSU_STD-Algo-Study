#include <iostream>
using namespace std;

int main(void)
{
	int N, K, A, B;
	bool n, k;

	cin >> N >> K;

	n = (N >= 1 && N <= 100000);
	k = (K >= 1 && K <= 10000);

	if (n == true && k == true)
	{
		for (int i = 1;i <= N;i++)
		{
			cin >> A >> B;
			if (A < 0 && B>10000)
				return 0;

			if (i == N)
				cout << "비와이";
		}
	}

	return 0;
}
